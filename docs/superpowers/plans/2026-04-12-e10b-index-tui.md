# E10b: TUI 内索引构建 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 C++ 索引构建能力集成进 devpilot，用户通过 TUI 斜杠命令 `/index <path>` 触发，无需外部 CLI 工具。

**Architecture:** 三层实现：L1-Command（`.devpilot/commands/index.md` 斜杠命令） → BashTool（`devpilot index <path>`）→ L3-Patch（`src/cli/cmd/index-cmd.ts` yargs 子命令）+ L2-Plugin（`cpp-code-search.ts` 改用 `spawnSync(process.execPath)`）。

**Tech Stack:** TypeScript, Bun, yargs, child_process.spawnSync

---

## 文件清单

| 文件 | 动作 |
|------|------|
| `scripts/cpp-indexer/index.ts` | 修改：`main()` → `export async function buildMain(codeRoot: string)` |
| `scripts/cpp-indexer/search.ts` | 新增：`searchMain({indexDir, query, topK})` 检索子命令 |
| `packages/opencode/src/cli/cmd/index-cmd.ts` | 新增：yargs 子命令（build + search 双模式） |
| `packages/opencode/src/index.ts` | 修改：注册 IndexCommand |
| `.devpilot/commands/index.md` | 新增：`/index` 斜杠命令模板 |
| `.devpilot/tools/cpp-code-search.ts` | 修改：删除动态 import，改用 spawnSync |

---

### Task 1: 修改 scripts/cpp-indexer/index.ts — 导出 buildMain

**Files:**
- Modify: `scripts/cpp-indexer/index.ts`

- [ ] **Step 1: 修改 main() 为 export async function buildMain(codeRoot: string)**

将当前 `async function main()` 改为接受参数的导出函数，移除自动调用，添加 `import.meta.main` 开发态入口。

完整替换文件末尾的 `main()` 函数签名和调用：

```typescript
// 将 "async function main() {" 这一行
// 改为：
export async function buildMain(codeRoot: string) {
  // 删除原来读取 process.argv 的逻辑（前5行）：
  // const args = process.argv.slice(2)
  // const codeRoot = args[0]
  // if (!codeRoot) { ... }
  // const modelArg = args.find(...)...
  // const modelName = modelArg as ModelName
  
  // 改为直接使用参数传入的 codeRoot，model 默认 MINILM：
  const modelName: ModelName = "MINILM"
  const codeRootAbs = path.resolve(codeRoot)
  // 以下逻辑不变...
}

// 开发态入口（仅 bun run index.ts 直接运行时执行）
if (import.meta.main) {
  const codeRoot = process.argv[2]
  if (!codeRoot) {
    console.error("用法: bun run index.ts <代码库路径>")
    process.exit(1)
  }
  buildMain(codeRoot).catch((e) => {
    console.error(e)
    process.exit(1)
  })
}
```

- [ ] **Step 2: 验证文件语法正确**

```bash
cd /Users/justbin/project/opensource/opencode
bun run --cwd . scripts/cpp-indexer/index.ts --help 2>&1 | head -5
```
预期：输出"用法: ..."或直接返回（不报语法错误）。

- [ ] **Step 3: Commit**

```bash
git add scripts/cpp-indexer/index.ts
git commit -m "refactor(cpp-indexer): export buildMain(codeRoot) for programmatic use"
```

---

### Task 2: 新增 scripts/cpp-indexer/search.ts

**Files:**
- Create: `scripts/cpp-indexer/search.ts`

- [ ] **Step 1: 创建 search.ts**

```typescript
/**
 * C++ 代码库语义检索子命令
 * 供 devpilot index --search 调用，读取索引，输出 JSON 到 stdout
 */
import path from "path"
import { existsSync, readFileSync } from "fs"
import { Bm25Index } from "./bm25"
import { VectorStore } from "./vector-store"
import { initEmbedder, embed } from "./embedder"
import { rrfFuse } from "./rrf"
import type { CodeChunk, IndexMetadata } from "./types"

export async function searchMain({
  indexDir,
  query,
  topK,
}: {
  indexDir: string
  query: string
  topK: number
}) {
  const metaPath = path.join(indexDir, "metadata.json")
  if (!existsSync(metaPath)) {
    console.error(JSON.stringify({ error: `索引不存在: ${indexDir}，请先运行 /index <代码库路径>` }))
    process.exit(1)
  }

  const meta: IndexMetadata = JSON.parse(readFileSync(metaPath, "utf-8"))
  const chunksPath = path.join(indexDir, "chunks.jsonl")
  const allChunks: CodeChunk[] = readFileSync(chunksPath, "utf-8")
    .trim()
    .split("\n")
    .filter(Boolean)
    .map((l) => JSON.parse(l))

  const bm25 = new Bm25Index(path.join(indexDir, "bm25.db"))
  const modelId = await initEmbedder("MINILM")
  const store = new VectorStore(modelId)
  store.load(path.join(indexDir, "vectors.bin"), path.join(indexDir, "vectors.idmap"))

  const [bm25Results, vectorResults] = await Promise.all([
    Promise.resolve(bm25.search(query, 20)),
    embed(query).then((vec) => store.search(vec, 20, allChunks)),
  ])
  bm25.close()

  const results = rrfFuse(bm25Results, vectorResults, topK)

  const output = {
    index_info: {
      code_root: meta.code_root,
      built_at: meta.built_at,
      chunk_count: meta.chunk_count,
    },
    results: results.map((r, i) => ({
      rank: i + 1,
      file: r.chunk.file,
      class: r.chunk.class,
      function: r.chunk.function,
      signature: r.chunk.signature,
      lines: `${r.chunk.start_line}-${r.chunk.end_line}`,
      score: r.score,
      label: r.label,
      doc_comment: r.chunk.doc_comment || null,
    })),
  }

  console.log(JSON.stringify(output))
}
```

- [ ] **Step 2: Commit**

```bash
git add scripts/cpp-indexer/search.ts
git commit -m "feat(cpp-indexer): add searchMain() for subprocess-based retrieval"
```

---

### Task 3: 新增 packages/opencode/src/cli/cmd/index-cmd.ts

**Files:**
- Create: `packages/opencode/src/cli/cmd/index-cmd.ts`

- [ ] **Step 1: 创建 index-cmd.ts**

```typescript
// CUSTOM: DevPilot — C++ 代码库索引构建子命令
// 用法（build 模式）：devpilot index <代码库路径> [--model=MINILM|BGE_M3]
// 用法（search 模式）：devpilot index <ignored> --search --index-dir=<dir> --query=<text> [--top-k=5]
// build 模式：由 /index TUI 命令通过 BashTool 调用，也可直接在终端使用
// search 模式：由 cpp-code-search.ts 通过 spawnSync(process.execPath) 调用
import { cmd } from "./cmd"

export const IndexCommand = cmd<
  {},
  {
    path: string
    model?: string
    search?: boolean
    "index-dir"?: string
    query?: string
    "top-k"?: number
  }
>({
  command: "index <path>",
  describe: "构建 C++ 代码库的离线语义索引",
  builder: (yargs) =>
    yargs
      .positional("path", {
        type: "string",
        demandOption: true,
        describe: "C++ 代码库根目录",
      })
      .option("model", {
        type: "string",
        default: "MINILM",
        describe: "Embedding 模型（MINILM|BGE_M3）",
      })
      // 以下选项仅供 cpp-code-search.ts 内部调用，隐藏不展示给用户
      .option("search", { type: "boolean", hidden: true })
      .option("index-dir", { type: "string", hidden: true })
      .option("query", { type: "string", hidden: true })
      .option("top-k", { type: "number", default: 5, hidden: true }),
  handler: async (argv) => {
    if (argv.search) {
      // 检索模式（供 cpp-code-search.ts subprocess 调用）
      const { searchMain } = await import("../../../scripts/cpp-indexer/search")
      await searchMain({
        indexDir: argv["index-dir"]!,
        query: argv.query!,
        topK: argv["top-k"]!,
      })
    } else {
      // 构建模式（用户通过 /index 命令触发）
      const { buildMain } = await import("../../../scripts/cpp-indexer/index")
      await buildMain(argv.path)
    }
  },
})
```

- [ ] **Step 2: Commit**

```bash
git add packages/opencode/src/cli/cmd/index-cmd.ts
git commit -m "feat(devpilot): add IndexCommand yargs subcommand for C++ indexing"
```

---

### Task 4: 修改 packages/opencode/src/index.ts — 注册 IndexCommand

**Files:**
- Modify: `packages/opencode/src/index.ts`

- [ ] **Step 1: 在 import 区域末尾添加 IndexCommand import**

在第 37 行 `import { PluginCommand } from "./cli/cmd/plug"` 之后添加：

```typescript
// CUSTOM: DevPilot — 注册索引构建命令
import { IndexCommand } from "./cli/cmd/index-cmd"
```

- [ ] **Step 2: 在 .command(SessionCommand) 之后注册 IndexCommand**

在 `// CUSTOM: DevPilot lockdown — 非交付功能，客户界面不暴露` 注释块附近、`.fail(` 之前添加：

```typescript
  // CUSTOM: DevPilot — 注册索引构建命令（内部子命令，不显示在用户面板）
  .command(IndexCommand)
```

- [ ] **Step 3: 验证 TypeScript 编译通过**

```bash
cd /Users/justbin/project/opensource/opencode
bun run --cwd packages/opencode tsc --noEmit 2>&1 | head -20
```
预期：无错误（或只有与本次改动无关的已有错误）。

- [ ] **Step 4: Commit**

```bash
git add packages/opencode/src/index.ts
git commit -m "feat(devpilot): register IndexCommand in CLI"
```

---

### Task 5: 新增 .devpilot/commands/index.md

**Files:**
- Create: `.devpilot/commands/index.md`

- [ ] **Step 1: 创建 index.md**

```markdown
---
name: index
description: 为指定 C++ 代码库构建离线语义索引，支持后续需求-代码关联检索
---

# /index 命令

用户输入 `/index <代码库路径>` 时，执行以下操作：

## 执行步骤

1. 从 `$ARGUMENTS` 提取代码库路径（若未提供，询问用户）
2. 验证路径存在（若不存在，立即报错，不执行构建）
3. 调用 BashTool 执行索引构建，**必须传入 timeout: 600000**（10 分钟）：
   ```
   devpilot index <代码库路径>
   ```
4. 实时展示 BashTool 输出的进度（5 个阶段：扫描→解析→BM25→Embedding→HNSW）
5. 构建完成后，告知用户：
   - 索引位置：`<代码库路径>/.opencode-index/`
   - 文件数和代码单元数（从 BashTool 输出解析）
   - 下一步：在该目录启动 devpilot 后可直接使用需求检索

## 注意事项

- 首次运行会下载 Embedding 模型（~90MB），需要网络，之后离线可用
- 大型项目（>10 万行）构建时间可能超过 5 分钟，请耐心等待
- 若路径不存在，立即提示错误，不要执行构建
- BashTool 的 timeout 参数**必须设置为 600000**（毫秒），否则默认 2 分钟会超时
```

- [ ] **Step 2: 验证命令文件可被 devpilot 识别**

```bash
cd /Users/justbin/project/opensource/opencode
bun run --cwd packages/opencode --conditions=browser src/index.ts --help 2>&1 | head -5
```
预期：正常启动（不报错）。命令面板在 TUI 中输入 `/` 后应可见 `index` 条目。

- [ ] **Step 3: Commit**

```bash
git add .devpilot/commands/index.md
git commit -m "feat(devpilot): add /index slash command for TUI-triggered C++ indexing"
```

---

### Task 6: 修改 .devpilot/tools/cpp-code-search.ts — 替换动态 import 为 subprocess

**Files:**
- Modify: `.devpilot/tools/cpp-code-search.ts`

- [ ] **Step 1: 替换 import 区域**

删除：
```typescript
import type { IndexMetadata, CodeChunk } from "../../scripts/cpp-indexer/types"
```

新增（在 `import { existsSync, readFileSync } from "fs"` 前）：
```typescript
import { spawnSync } from "child_process"
```

同时添加内联类型定义（替换被删除的 import type）：
```typescript
interface IndexMetadata {
  code_root: string
  built_at: string
  file_count: number
  chunk_count: number
  embedding_model: string
  version: string
}
```

- [ ] **Step 2: 删除 loadChunks() 函数**

删除整个 `loadChunks()` 函数（第 38-44 行）：
```typescript
/** 从 chunks.jsonl 加载全量 chunk 元数据 */
function loadChunks(): CodeChunk[] {
  const lines = readFileSync(CHUNKS_JSONL_PATH, "utf-8")
    .trim()
    .split("\n")
    .filter(Boolean)
  return lines.map((l) => JSON.parse(l))
}
```

- [ ] **Step 3: 删除未使用的常量**

删除 `loadChunks()` 用到的常量（BM25_DB_PATH、VECTORS_BIN_PATH、VECTORS_IDMAP_PATH、CHUNKS_JSONL_PATH），因为 subprocess 模式下这些路径在 search.ts 内部处理。只保留：
```typescript
const INDEX_DIR = path.join(process.cwd(), ".opencode-index")
const METADATA_PATH = path.join(INDEX_DIR, "metadata.json")
```

- [ ] **Step 4: 替换 execute 函数体**

将原有的 `execute: async (args) => { ... }` 内容替换为：

```typescript
  execute: async (args) => {
    // 1. 检查索引元信息
    const check = checkIndex()
    if (!check.ok) {
      return JSON.stringify({ error: check.error })
    }

    // 2. 拼接查询文本
    const queryText = [
      args.requirement.description,
      ...(args.requirement.acceptance_criteria ?? []),
    ].join(" ")

    // 3. 通过 process.execPath 调用自身 index --search 子命令
    //    process.execPath 在 Bun compiled binary 中指向 devpilot binary 本身
    const result = spawnSync(
      process.execPath,
      [
        "index",
        "_placeholder_",  // <path> positional arg（search 模式下忽略）
        "--search",
        `--index-dir=${INDEX_DIR}`,
        `--query=${queryText}`,
        `--top-k=${args.top_k}`,
      ],
      { encoding: "utf-8", timeout: 10000 },
    )

    if (result.status !== 0) {
      return JSON.stringify({
        error: result.stderr || "检索失败，请确认索引已构建（使用 /index <路径>）",
      })
    }

    const searchResult: { index_info: any; results: any[] } = JSON.parse(result.stdout)
    const meta = check.meta!
    const indexAge = Math.round(
      (Date.now() - new Date(meta.built_at).getTime()) / 1000 / 60,
    )

    const mdTable = [
      `## 需求-代码关联检索结果（${args.requirement.id}）`,
      "",
      `**查询：** ${queryText.slice(0, 80)}${queryText.length > 80 ? "..." : ""}`,
      `**索引：** ${meta.code_root}（${indexAge} 分钟前构建，共 ${meta.chunk_count} 个代码单元）`,
      "",
      "| # | 文件 | 类 | 函数 | 关联度 | 等级 |",
      "|---|------|----|------|--------|------|",
      ...searchResult.results.map(
        (r: any) =>
          `| ${r.rank} | \`${r.file}\` | ${r.class ?? "-"} | \`${r.function ?? "-"}\` | ${r.score} | **${r.label}** |`,
      ),
    ].join("\n")

    return JSON.stringify({
      json: {
        requirement_id: args.requirement.id,
        query: queryText,
        ...searchResult,
      },
      markdown: mdTable,
    })
  },
```

- [ ] **Step 5: 更新 checkIndex 错误提示**

将 `checkIndex()` 中的错误信息更新为引导用户使用 `/index` 命令：
```typescript
return {
  ok: false,
  error: `索引未构建。请在 devpilot TUI 中输入：/index <C++代码库路径>\n索引会写到 <C++代码库路径>/.opencode-index/\n然后在该 C++ 代码库根目录启动 devpilot 即可自动读取索引。`,
}
```

- [ ] **Step 6: Commit**

```bash
git add .devpilot/tools/cpp-code-search.ts
git commit -m "feat(devpilot): replace dynamic imports in cpp-code-search with spawnSync subprocess"
```

---

## 验证

```bash
# 开发态验证
cd /Users/justbin/project/opensource/opencode
bun run --cwd packages/opencode --conditions=browser src/index.ts index --help
# 预期：展示 "构建 C++ 代码库的离线语义索引" 帮助信息

# 全量构建验证
bun run build --single --skip-embed-web-ui
./dist/devpilot-ai-darwin-*/bin/devpilot index --help
# 预期：同上
```

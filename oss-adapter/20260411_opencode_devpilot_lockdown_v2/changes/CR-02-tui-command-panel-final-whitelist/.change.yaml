cr_id: CR-02
slug: tui-command-panel-final-whitelist
status: completed
type: discovery_fix
priority: P1
related_reqs: [REQ-17, REQ-21]
related_epic: E5
base_spec: specs/E5-lockdown-v2-design.md
discovered_at: "2026-04-11"
implemented_at: "2026-04-11"
implemented_by: manual_verification

summary: >
  E5 审计遗漏：app.tsx 中 /agents /mcps /models 三个 CommandOption 命令
  未加 hidden:true；command/index.ts 内置 /review 命令未注释。
  四个命令均在用户界面可见，不在 R01-R09 交付范围内。

affected_files:
  - path: "packages/opencode/src/cli/cmd/tui/app.tsx"
    action: modified
    lines: "~502-513 (models), ~554-565 (agents), ~566-576 (mcps)"
    change: "各加 hidden: true + CUSTOM 注释"
  - path: "packages/opencode/src/command/index.ts"
    action: modified
    lines: "~95-104"
    change: "注释掉 commands[Default.REVIEW] 块"

command_whitelist_after_fix:
  kept:
    - "/copy      — 复制对话记录（基础操作）"
    - "/editor    — 外部编辑器写 prompt（Phase 6 保留）"
    - "/exit      — 退出应用（必须）"
    - "/export    — 导出对话记录（Phase 6 保留）"
    - "/gen-test  — R04 测试生成交付命令"
    - "/help      — 快捷键帮助（Phase 6 保留）"
    - "/init      — 生成 AGENTS.md（用户明确保留）"
    - "/new       — 新建会话（基础操作）"
    - "/rename    — 重命名会话（Phase 6 保留）"
    - "/structurize-req — R01 需求结构化交付命令"
  hidden:
    - "/agents  — Agent 切换，R01-R09 无此场景"
    - "/mcps    — MCP 管理，R01-R09 无此场景"
    - "/models  — 模型切换，由管理员预配置"
    - "/review  — Code review，R01-R09 无此需求"

root_cause: >
  E5 REQ-17/21 只覆盖了 session/index.tsx 中的 session 管理命令，
  未审计 app.tsx 中的 Agent 类 CommandOption（agents/mcps/models）；
  command/index.ts 的 Default.REVIEW 内置命令也未纳入屏蔽清单。

upgrade_note: >
  升级时检查 app.tsx CommandOption 列表（Agent category 下）是否有新增 slash 命令；
  检查 command/index.ts Default.* 内置命令是否有新增项。
